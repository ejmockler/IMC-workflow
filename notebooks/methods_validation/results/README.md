# methods_validation/results/ — orphan November 2025 cache

**Status (2026-05-07): legacy / stale.**

The CSVs and PNGs in this directory (`effect_sizes_mouse_level.csv`, `effect_sizes_corrected_mouse_level.csv`, `mouse_level_data.csv`, `power_analysis*.png`, `power_analysis_markers.csv`, `power_curve*.csv`, `power_curves.png`, `sample_size_recommendations.csv`, `variance_components.csv`) are pre-Phase-7 outputs from an earlier methods-validation pass. They use single-effect-size framing (`d = ...`) rather than the current Hedges' g + three-prior Bayesian shrinkage range. No current notebook, script, or doc references these files.

The reviewer-facing analysis is in `results/biological_analysis/temporal_interfaces/endpoint_summary.csv` (840 rows × 46 cols, post-remediation) and `results/power_analysis/sample_size_requirements.csv` (current cell-type-level power table). The empty `cross_sectional_kidney_injury/` subdirectory is also a leftover and may be removed.
